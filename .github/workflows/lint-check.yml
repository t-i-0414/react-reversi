name: Lint Check

on:
  pull_request:
    branches:
      - master
      - develop
      - release/**

jobs:
  lint-check-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Install dependencies
        run: pnpm install
      - name: Lint Check run
        run: pnpm lint
