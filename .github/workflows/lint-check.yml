name: Lint Check

on:
  pull_request:
    branches:
      - master
      - develop
      - release/**

jobs:
  lint-check-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - uses: pnpm/action-setup@v2.2.4
        with:
          version: 7.28.0
      - name: Install dependencies
        run: pnpm install
      - name: Lint Check run
        run: pnpm lint
